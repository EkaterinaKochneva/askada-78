mixin footer(props = { class: [], attrs: {} })
  -
    let classes = ['footer'];
    let attrs = props.attrs ? props.attrs : {};

    if (Array.isArray(props.class)) {
        props.class.forEach((key) => {
        classes.push(key);
        })
    }

  footer(class= classes)&attributes(attrs) Подвал

  include /ui/modal/index